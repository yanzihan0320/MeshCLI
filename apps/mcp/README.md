# CaudalFlow MCP App

Reserved integration point for future MCP resources and widgets.

The current Copilot flow does not require an MCP server yet: the BFF connects the
frontend to the LangGraph agent, and canvas actions are exposed through
CopilotKit frontend tools. Add MCP resources here when CaudalFlow needs external
widgets, resources, or tool servers beyond the local canvas state.
